$x \in [a,b]$ means $a \leq x \leq b$.

$x \in (a,b)$ means $a < x < b$.

$\set{a \text{ such that } a = b} = \set{a | a = b} = \set{a : a = b}$.

$n \text{ choose } k = \binom{n}{k}= \frac{n!}{k!(n-k)!} = C_n^k = \actsymb[n]{C}{k} = \actsymb[][n]{C}{k} = C(n,k)$.

For Trigonometric Functions, $\sin^2(x) = \left(\sin(x)\right)^2$, not $\sin(\sin(x))$. \\
Unless the exponent is $-1$ in which case, $y = \sin^{-1}(x) = \arcsin(x)$,
is defined $x = \sin(y)$.

i.i.d.r.v.'s means ``Idependent Identically Distributed Random Variables''.