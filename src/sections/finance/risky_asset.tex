TODO, Binomial Tree, $u, d$.

Let $\bm{r}$ be the one-step return for a risk-free investment,
where $d < r < u$.

Define the \textbf{Risk-Neutral Probability} $\bm{p_*} = \frac{r-d}{u-d}$,
where $0 < p_* < 1$ and $p_* (u-r) + (1-p_*)(d-r) = 0$.

\(
    E_*(S(t+1) | S(t)) = S(t)(1+r)
\)

TODO $r_*, q_*$.

TODO Continuous-Time Limit

\(
    \ln(1+u) = m \tau + \sigma \sqrt{\tau},
    \ln(1+d) = m \tau - \sigma \sqrt{\tau},
\)